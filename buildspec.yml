version: 0.1
phases:
  install:
  runtime-versions:
      Python: python:3.5.2
    commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket itzmd-lambda-pipeline --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputSamTemplate.yaml
